# Variables that can be put on the x and y axes
axis_vars <- c(
  "Number of Votes" = "numVotes",
  "Year Released"  = "year",
  "Average imdb Rating" = "averageRating",
  "Runtime (mins)" = "runtimeMinutes",
  "Budget (US$)" = "budget",
  "Revenue (US$)" = "revenue"
)